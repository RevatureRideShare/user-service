server:
  port: 8090

spring:
  application:
    name: user-service

  cloud:
    config:
      uri: src/main/resources/application.yml
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
